# Archive Manifest

Source repository:
https://github.com/Rodert/wechat-win-versions

Original author:
Rodert

Backup repository:
https://github.com/WatchKitty/wechat-win-versions

Backup policy:

- Git history: preserved
- Branches: preserved
- Tags: preserved
- Historical GitHub Releases: NOT mirrored
- Historical Release Assets: NOT mirrored
- Future releases: archived independently by this repository

Repository visibility: PUBLIC (deliberate, reviewed 2026-09-11)

- GitHub Pages is only available for private repositories on GitHub Pro / Team /
  Enterprise; on GitHub Free the repository must be public. Going private would
  therefore break the online download page outright.
- GitHub Actions is free and unlimited for public repositories. A private
  repository would consume the shared 2,000 free minutes/month instead. This
  repository uses roughly 20-60 minutes/month, so private would not have cost
  money, but it would eat into the shared quota.
- GitHub release assets are not billed: per GitHub Support, release assets live
  outside the Git repository, do not count toward repository size limits, and
  bandwidth is neither capped nor charged.
- Note that making the repository private would not have made the published
  page private either - GitHub Pages sites are publicly reachable on the
  internet even when built from a private repository.
- The visibility decision is recorded here because the trade-off is real: a
  public repository clears the cost and functionality questions but keeps the
  redistribution question open. See `NOTICE` for the licensing position.

Initial source main SHA:
e6b01ab039b6912e8e08d6e8e852dcc978bb2078

Current backup main SHA:
not pinned here on purpose - a SHA recorded in a file is stale the moment it is
committed. Use `git log` / `git rev-parse HEAD` for the live value.

Backup main SHA immediately after the mirror push:
e6b01ab039b6912e8e08d6e8e852dcc978bb2078  (verified equal to source main)

Backup main SHA at the first independent release (release-cache commit):
b3676966b143131eff530d84fef090f328f41125

Everything after that commit is this repository's own maintenance work
(README / ARCHIVE_* / index.html / NOTICE / destversion.yml). Run `git log` for
the current HEAD; the field above is deliberately not kept up to date.

Initial source main SHA == backup initial main SHA:
VERIFIED (e6b01ab039b6912e8e08d6e8e852dcc978bb2078)

Refs copied:

- branches: 1 (refs/heads/main)
- tags: 28
- refs/pull: not present upstream, not copied
- remote refs compared with `git ls-remote`: IDENTICAL

First independent workflow run:
https://github.com/WatchKitty/wechat-win-versions/actions/runs/34598240270
timestamp: 2026-09-11T12:18:23Z (UTC)
workflow: Wechat Dest Version (destversion.yml)
result: success

First independently archived WeChat release:
tag: v4.1.13.65
title: Wechat v4.1.13.65
url: https://github.com/WatchKitty/wechat-win-versions/releases/tag/v4.1.13.65
published: 2026-09-11T12:19:05Z (UTC)
asset: WeChatSetup-4.1.13.65.exe (256,020,400 bytes)
SHA256: a1978685c5d12ee6adc372a6348ffe1c9b2ca95a59787e4ed66279436b57c5e1
source download URL: https://dldir1v6.qq.com/weixin/Universal/Windows/WeChatWin_4.1.13.exe
official Last-Modified: Wed, 09 Sep 2026 02:21:00 GMT

Release cache (releases.json):
reset to `[]` before the first independent run
after the first independent run: contains exactly 1 entry (v4.1.13.65, owned by this repository)
upstream release metadata retained: NO

Collector data source:
https://pc.weixin.qq.com/ (official page)
https://dldir1v6.qq.com/ (official CDN)
runtime dependency on the upstream Rodert repository: NONE

GitHub Actions:
workflow enabled: yes (Wechat Dest Version, active)
repository workflow permissions: contents write (default_workflow_permissions = write)
scheduled destructive mirror (git push --mirror on cron): NOT configured

Historical release migration:
Disabled by design.

## Maintenance changes applied to this backup

These are the only files this backup modified relative to upstream. The
version-discovery logic is untouched.

- `.github/workflows/destversion.yml`
  - added `actions: write` to the workflow permissions
  - added a final step that dispatches `pages.yml` (`gh workflow run pages.yml`)
    after the release cache is committed, so the online page refreshes
    automatically. Necessary because a `git push` made with `GITHUB_TOKEN` does
    not trigger other workflows.
- `index.html`
  - the repository links now point to this backup repository
    (`WatchKitty/wechat-win-versions`)
  - the upstream URL is preserved on the page as `Original repo` for attribution
- `NOTICE` (new)
  - records that the upstream project declares no license, that all rights stay
    with the original author, and that this backup grants no license
- `releases.json`
  - reset to `[]`, then regenerated by this repository's own collector
- `README.md`, `ARCHIVE_NOTICE.md`, `ARCHIVE_MANIFEST.md`
  - independent-backup declarations and attribution
- `scripts/destVersionRelease.sh`
  - **unchanged** (data source: `pc.weixin.qq.com`, no upstream dependency)
